namespace Raon.Core.FrontEnd;

public enum TokenType
{
    TOKEN_IDENTIFIER,
    TOKEN_INTEGER,
    TOKEN_REAL,
    TOKEN_STRING,

    TOKEN_PLUS,
    TOKEN_MINUS,
    TOKEN_ASTERISK,
    TOKEN_SLASH,
    TOKEN_LPAREN,
    TOKEN_RPAREN,
    TOKEN_ASSIGN,

    TOKEN_EOL,
    TOKEN_EOF,

    TOKEN_UNKNOWN,
}